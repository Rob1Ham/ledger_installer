[workspace]
members = ["ledger_manager", "cli", "gui", "web"]
resolver = "2"

[workspace.package]
edition = "2021"
license-file = "LICENCE"
readme = "README.md"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
